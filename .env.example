# Environment variables
PORT=3000
NODE_ENV=development

# Google Safe Browsing API Key for Threat Intelligence (URL Scanner)
# Optional: If not provided, the URL scanner will clearly display
# "Threat intelligence unavailable" while keeping ML detection fully functional.
GOOGLE_SAFE_BROWSING_API_KEY=
